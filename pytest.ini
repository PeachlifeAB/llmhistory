[pytest]
testpaths = tests
norecursedirs = external-reference-code .agentsensei
addopts = -m "not integration"
markers =
    integration: tests that touch real OpenCode local storage or shell integration flows
    unit: fast isolated tests
